# [Label](https://semantic-ui.com/elements/label.html)

```
	<label class="ui label">
		<slot />
	</label>
```

The component contains no logic by itself but is taken into consideration when used within another component